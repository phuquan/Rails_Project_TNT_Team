<!--<link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">-->
<link rel="stylesheet" href="//blueimp.github.io/Gallery/css/blueimp-gallery.min.css">
<%= stylesheet_link_tag 'bootstrap-image-gallery.min.css', media: 'all', 'data-turbolinks-track' => true %>

<div class="container">
<p id="notice"><%= notice %></p>

<h1 class="big-text text-center scrollimation fade-in in">Listing Image</h1>

<div id="links">
<%
	index = 0;
	@images.each do |image| %>
	<%
		if ((index + 1) % 4) == 1
	%>
	<div class = "row" style="padding: 5px;">
	<%
		end
		index += 1
	%>
	<div class="col-lg-3" style="box-shadow: 1px 1px 2px 1px #888888 !important;">
		<div class="row">
		   <a href="<%=image.url%>" title="<%= image.name %>" class="image_element" id="<%= image.id %>" data-gallery>
		      <img src="<%= image.url %>" width="280px" height="280px">
		   </a>
	   </div>
	   <div class="row">
	   		Posted by <a href="/users/<%= image.user_id %>"><%= image.user.email %></a> 
	   </div>
	   <div class="row">
	   		at <%= image.created_at.strftime("%I:%M%p") + " on " + image.created_at.strftime(" %Y/%m/%d") %>
	   </div>
	 </div>
	  <%
		if ((index + 1) % 4) == 1
	%>
	</div>
	<%
	  end
	%>
<%
	end
%>
</div>
<br>
<% @images.each do |image| %>
<%= link_to 'Destroy', image, method: :delete, data: { confirm: 'Are you sure?' } %>
<%end%>
<%= render 'dialog' %>
<%= render 'imagedialog' %>
</div>
<!--<script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>-->
<script src="//blueimp.github.io/Gallery/js/jquery.blueimp-gallery.min.js"></script>
<%= javascript_include_tag 'bootstrap-image-gallery.js', 'data-turbolinks-track' => false %>
